name: Release version
on:
  release:
    types: [released]

jobs:
  merge-to-master:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{github.event.release.target_commitish}}

      - name: Set Git config
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "Github Actions"
      
      - name: Merge development -> main / master
        run: |
        
          git fetch origin
          git checkout master || git checkout main || {echo "No master or main branch"; exit 1}
          git merge  --no-ff ${{github.event.release.target_commitish}} -m "Release ${{github.event.release.tag_name}}"
          git push
